var React = require('react');

var Leaderboard = React.createClass({
  render : function () {
    return (
      <div>
        <h1 style={{color:'white'}}>This page is for Leaderboard!</h1>
      </div>
    );
  }
});

module.exports = Leaderboard;
